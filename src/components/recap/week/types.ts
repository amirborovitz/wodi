import type { WeekPosterData } from '../../../hooks/useWeekPosterData';

/** Every week skin renders the same week and takes nothing else. */
export interface WeekSkinProps {
  week: WeekPosterData;
}
