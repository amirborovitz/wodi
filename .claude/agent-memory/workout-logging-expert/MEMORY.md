- [repairUndercountedBreakdown must skip buy-in/cash-out](repair_undercounted_buy_in_cash_out.md) — when an exercise has no sections[] but its movements[] contains a cash-out (perRound:false), the repair function multiplies its totals by round count; check role/perRound/countingMode before multiplying.
- [computeTeamShare divides per-round reps](computeteamshare_divides_per_round_reps.md) — partner RFT poster rows show per-round reps halved (5→3) because artifactRowToPosterLine treats row.primary as a team total and divides by teamSize.
- [sections[] shadow top-level movements](sections_shadow_toplevel_movements.md) — sections[] make workload+poster ignore top-level movements[]; per-tier cardio buy-ins (run before each 3/2/1 tier) normalized to buy_in sections in the post-processor (normalizePerTierBuyIns), not a prompt rule (AI non-deterministic); rounds=1 tier titles gated on hasMultiRoundTier.
- [Per-movement ladder + board fidelity](per_movement_ladder_and_board_fidelity.md) — poster mirrors the board's compact notation (don't collapse OR expand); per-movement independent rep ladders render each movement's own scheme via sections-per-round + hasSameMovementsEveryRound; double implement is never a barbell.
- [Hero cardio + strength rules](hero_cardio_and_strength_rules.md) — computeHeroResult: strength-on-a-clock heroes peak weight (shared isWeightedStrengthWork predicate), and counted cardio (calories AND distance) must beat the generic rep fallback; cardio guard is movement-based, not format-gated.
- [Part split happens at structuring, not just segmentation](part_split_happens_at_structuring.md) — structurePart re-runs the full prompt per part and can split one part into multiple exercises; test the part's own text 3x, segmentation-only checks are insufficient.
